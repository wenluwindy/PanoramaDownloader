# 全景图检测与下载工具 — 软件开发文档

> 版本：v0.2（已确认）  
> 日期：2026-09-20  
> 状态：**需求已确认，可进入开发**

---

## 0. 已确认决策（摘要）

| 项 | 结论 |
|----|------|
| 运行平台 | **仅 Windows**（Windows 10/11，x64） |
| 技术路线 | **.NET 8 + WPF + WebView2** |
| 一期数据源 | **仅 720 云**（用户有权访问的作品） |
| 导出 | **瓦片包 + equirectangular 整图**（两者都要） |
| 预览 | **一期缩略图**；3D 交互预览放到二期 |
| 使用边界 | **仅授权/自有内容**；需登录时在软件内提供 **WebView2 登录页** |
| 产品名称 | 全景图下载工具（暂定） |

---

## 1. 文档目的

本文档为已确认的产品与技术方案基线。开发、验收均以本文档为准；变更需先改文档再改代码。

---

## 2. 产品概述

### 2.1 一句话描述

在 Windows 桌面端输入 720 云作品 URL，必要时在内嵌页面登录，自动识别全景场景与瓦片资源，展示缩略图预览，并导出原始瓦片包与等距柱状整图。

### 2.2 目标用户

- 个人用户：对自己有权使用的 720 云作品做本地存档  
- 内容创作者 / 设计师：导出素材做二次制作  
- 企业内部：归档自有展厅/房产全景  

**定位：** 个人与小团队使用的本地桌面工具，非在线爬虫服务。

### 2.3 非目标（一期明确不做）

- 破解 720 云付费/加密/DRM、绕过验证码或防盗链对抗  
- Krpano / 通用瓦片 / 其他平台适配（列入二期）  
- 软件内 3D 拖拽全景预览（二期）  
- 浏览器扩展、跨平台（macOS/Linux）  
- 批量商用爬取、公开分享他人作品的托管能力  

> **合规原则：** 仅用于用户有权访问或自有内容；首次启动展示使用声明；不上传用户 Cookie 到第三方服务器。

---

## 3. 核心使用流程

```
输入 720 云作品 URL
        ↓
WebView2 加载页面
        ↓
若需登录 → 弹出/嵌入登录页，用户手动登录成功后继续
        ↓
捕获网络请求 + 解析页面/接口 → 生成场景与瓦片清单（manifest）
        ↓
展示场景列表 + 缩略图预览
        ↓
用户选择导出：瓦片包 / 整图 / 两者
        ↓
并发下载 →（可选）拼接 equirectangular → 保存本地
```

---

## 4. 功能需求

### 4.1 一期（MVP）

| 编号 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| F1 | URL 输入与分析 | P0 | 支持 720 云作品页 HTTPS 链接 |
| F2 | 内嵌登录 | P0 | 检测到未登录/无权限时，用 WebView2 展示登录页；登录态仅存本地 WebView2 配置目录 |
| F3 | 720 云资源检测 | P0 | 识别作品标题、场景列表、瓦片/CDN 规则、缩略图地址 |
| F4 | 资源清单 | P0 | 场景名、层级数、瓦片数量估算、预估体积、来源信息 |
| F5 | 缩略图预览 | P0 | 列表/大图缩略图确认场景；不提供 3D 旋转 |
| F6 | 瓦片下载 | P0 | 并发下载、进度、失败重试、断点续传 |
| F7 | 导出瓦片包 | P0 | 规范化目录 + `manifest.json` |
| F8 | 导出整图 | P0 | 拼接为 equirectangular JPG（默认）/ PNG（可选） |
| F9 | 进度与日志 | P0 | 进度条、速度、失败列表、可复制日志 |
| F10 | 使用声明 | P0 | 首次启动确认；关于页免责声明 |
| F11 | 本地设置 | P1 | 下载目录、并发数、超时、重试、默认导出选项 |
| F12 | 任务历史 | P1 | 最近 URL、导出路径 |

### 4.2 二期（增强，不在本期开发）

| 编号 | 功能 |
|------|------|
| F20 | 软件内 3D 全景预览（拖拽/缩放） |
| F21 | Krpano、通用立方体/等距瓦片等其它源 |
| F22 | 多场景一键批量导出 |
| F23 | 热点 / 场景跳转元数据导出 |
| F24 | GPU 加速大图拼接 |
| F25 | 系统代理与高级自定义 Header |

### 4.3 功能细则

#### 4.3.1 720 云检测

以 **WebView2 真实加载 + 网络请求捕获** 为主，DOM/接口解析为辅：

1. 用户打开作品 URL；若跳转登录或接口返回未授权，引导至登录 WebView  
2. 登录成功后，监听 XHR/Fetch/图片请求，聚类 CDN 与瓦片 URL 模板  
3. 解析作品/场景相关接口或页面内嵌配置，得到场景 ID、名称、缩略图、多分辨率层级  
4. 输出统一 `PanoramaManifest`（见 §6.4）  

> 实现阶段需针对当前 720 云前端协议做专项逆向梳理（仅用于授权内容导出），并做成可替换的 `Yun720Detector`，以便平台改版时局部更新。

#### 4.3.2 登录

- 使用 **同一 WebView2 用户数据目录** 持久化 Cookie，避免每次重复登录  
- 提供「打开登录页」「退出登录（清 Cookie）」操作  
- **不**自动填充账号密码、**不**绕过验证码；由用户在官方页面自行完成登录  
- Cookie 仅存本机，不上传  

#### 4.3.3 预览（一期）

- 每个场景显示官方或捕获到的缩略图  
- 展示元信息：场景名、层级、预估分辨率/瓦片数  
- 无缩略图时显示占位图 + 文字说明  

#### 4.3.4 下载与导出

| 模式 | 产出 |
|------|------|
| 瓦片包 | 按场景分目录保存全部（或所选层级）瓦片 + `manifest.json` |
| 等距柱状整图 | 单张 2:1 JPG/PNG，可用系统看图或 PS/AE 打开 |
| 两者 | 同一任务连续产出上述两种 |

约束：

- 并发默认 8，可配置 2–16，降低限流风险  
- 单瓦片失败不中断整任务，结束后可重试失败项  
- 超大图（如宽边 > 16384）弹出内存/耗时警告，允许降级导出较低层级  
- 下载请求复用 WebView 会话中的 Cookie / Referer（在合法用户会话内）  

---

## 5. 支持范围（一期）

| 类型 | 一期 |
|------|------|
| 720 云 — 用户登录后可正常打开的作品 | **必须支持** |
| 720 云 — 完全公开无需登录的作品 | **必须支持** |
| 720 云 — 付费加密 / DRM / 无法在官方页观看的内容 | **不支持** |
| Krpano / 其它平台 | **二期** |

---

## 6. 技术方案（已锁定）

### 6.1 技术栈

| 层级 | 选型 |
|------|------|
| 运行时 | .NET 8 |
| UI | **WPF**（成熟稳定；WinUI 3 作为远期可选迁移，本期不采用） |
| 内嵌浏览器 | **Microsoft Edge WebView2** |
| HTTP 下载 | `HttpClient` + 信号量限流；必要时从 WebView 注入 Cookie |
| 图像拼接 | **ImageSharp** 或 **SkiaSharp**（大图分块写入，避免 OOM） |
| 打包 | `MSIX` 或单文件 `win-x64` 发布 + WebView2 Evergreen 依赖说明 |
| 最低系统 | Windows 10 1809+ / Windows 11，已安装 WebView2 Runtime |

**选定 WPF 而非 WinUI 3 的原因（本期）：** WebView2、桌面工具生态更成熟，打包与控件资料更多；仅 Windows 场景下 WPF 足够。

### 6.2 总体架构

```
┌──────────────────────────────────────────────────┐
│              WPF 主界面（MVVM）                    │
│   URL / 登录 / 场景列表 / 缩略图 / 进度 / 设置      │
└─────────────────────┬────────────────────────────┘
                      │
┌─────────────────────▼────────────────────────────┐
│                   核心引擎                         │
│  ┌────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │ WebView2   │  │ Yun720      │  │ Downloader │ │
│  │ Loader     │→ │ Detector    │→ │ + Stitcher │ │
│  │ + 请求捕获  │  │             │  │            │ │
│  └────────────┘  └─────────────┘  └────────────┘ │
│                      │                            │
│              PanoramaManifest                     │
└──────────────────────────────────────────────────┘
```

### 6.3 模块划分

| 模块 | 职责 |
|------|------|
| `App` / `Views` / `ViewModels` | WPF 界面与 MVVM |
| `Core.Loader` | WebView2 导航、登录态检测、请求/响应钩子 |
| `Core.Detectors.Yun720` | 720 云专用解析 → Manifest |
| `Core.Downloader` | 并发下载、续传、重试 |
| `Core.Stitcher` | 瓦片 → equirectangular |
| `Core.Models` | Manifest、任务状态 DTO |
| `Core.Storage` | 设置、历史、缓存目录 |
| `Tests` | 脱敏 fixture + 解析单测 |

### 6.4 统一数据模型（草案）

```json
{
  "sourceUrl": "https://www.720yun.com/t/xxxxxxxx",
  "detector": "yun720",
  "title": "作品标题",
  "scenes": [
    {
      "id": "scene_01",
      "name": "大厅",
      "projection": "cube-multires",
      "thumbnailUrl": "https://...",
      "levels": [
        {
          "level": 0,
          "tileSize": 512,
          "faceSize": 1024,
          "urlTemplate": "https://.../{face}/{level}/{y}/{x}.jpg"
        }
      ],
      "faces": ["l", "f", "r", "b", "u", "d"]
    }
  ],
  "headersHint": {
    "Referer": "https://www.720yun.com/"
  },
  "detectedAt": "2026-09-20T15:00:00+08:00"
}
```

下载器与拼接器只依赖 Manifest，便于二期增加其它 Detector。

### 6.5 检测与登录流程

1. 用户输入 URL → `Loader` 在 WebView2 中打开  
2. 若页面为登录流或接口指示未授权 → UI 切换到登录区域，等待用户完成登录  
3. 登录完成后重新加载作品 URL  
4. 收集网络请求与关键配置响应 → `Yun720Detector` 生成 Manifest  
5. UI 展示场景缩略图列表  
6. 用户选择场景与导出模式 → Downloader →（可选）Stitcher → 写盘  

### 6.6 拼接要点

- 按 720 云实际投影（多为立方体多分辨率）拼面，再转换为 equirectangular  
- 默认 JPEG quality 92；可选 PNG  
- 大图分条带/分块写出，限制峰值内存  
- 允许用户选择导出「最高层级」或「指定层级」  

---

## 7. 界面草案

### 7.1 主界面

```
┌────────────────┬─────────────────────────────────┐
│ 作品 URL       │                                 │
│ [分析]         │     当前场景缩略图（大图）         │
│ [登录/退出]    │                                 │
│                ├─────────────────────────────────┤
│ 场景列表       │ 标题 / 层级 / 预估大小             │
│  □ 场景A 缩略  │                                 │
│  □ 场景B 缩略  │ [导出瓦片] [导出整图] [全部导出]   │
│                │ 进度 ████████░░  62%             │
│ 日志           │                                 │
└────────────────┴─────────────────────────────────┘
```

### 7.2 登录

- 主界面按钮「登录 720 云」→ 打开内嵌 WebView2（或右侧面板）加载官方登录页  
- 登录成功后状态显示「已登录」；提供「退出登录」清除本地 WebView 数据  

### 7.3 设置

- 下载目录、并发数、超时、重试次数  
- 默认导出：仅瓦片 / 仅整图 / 两者  
- JPEG 质量、最大导出边长  
- WebView2 用户数据目录路径（高级）  

---

## 8. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 数千瓦片任务下 UI 可响应；下载在后台线程 |
| 稳定性 | 单瓦片失败可重试；支持断点续传 |
| 安全 | Cookie 仅本地；无云端账号体系 |
| 体积 | 安装包目标（不含 WebView2 Runtime）尽量 < 80MB |
| 日志 | 可导出脱敏诊断包（manifest + 错误日志） |
| 合规 | 首次启动使用声明；关于页免责声明 |

---

## 9. 分期计划

| 阶段 | 周期（估） | 交付 |
|------|------------|------|
| 阶段 0 | 已完成 | 需求确认（本文档 v0.2） |
| 阶段 1 | 3–5 天 | WPF 空壳 + WebView2 打开 720 云 + 登录态保持 |
| 阶段 2 | 1–2 周 | 请求捕获 + `Yun720Detector` → Manifest（需真实页面协议分析） |
| 阶段 3 | 1 周 | 瓦片下载 + 进度/重试/续传 |
| 阶段 4 | 1 周 | 拼接 equirectangular + 双模式导出 |
| 阶段 5 | 3–5 天 | 缩略图 UI 打磨、声明、安装包、基本测试 |

**MVP 验收标准：**

1. 公开作品：输入 URL → 出场景缩略图 → 可导出瓦片包与整图  
2. 需登录作品：软件内登录后，同上流程可用  
3. 失败瓦片可重试；日志可查看  
4. 导出的整图可在常见看图软件中按全景/平面正常打开  

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 720 云前端/接口改版 | 检测失败 | Detector 隔离；请求聚类兜底；版本化适配 |
| 签名 URL / Token 过期 | 下载中断 | 缩短任务耗时；失败时刷新页面后重试；复用登录 Cookie |
| 超大分辨率 OOM | 拼接崩溃 | 分块拼接；限制最大层级/边长 |
| 版权争议 | 产品风险 | 授权用途声明；不做破解；不提供盗抓能力 |
| WebView2 未安装 | 无法运行 | 安装包检测 Runtime，引导安装 Evergreen |

---

## 11. 建议仓库结构

```
全景图下载工具/
├── 软件开发文档.md
├── README.md
├── src/
│   ├── PanoramaDownloader.App/          ← WPF
│   └── PanoramaDownloader.Core/         ← 检测/下载/拼接
├── tests/
│   └── PanoramaDownloader.Tests/
└── pack/                                ← 发布脚本
```

---

## 12. 确认记录

| 项 | 内容 |
|----|------|
| 文档版本 | v0.2 |
| 确认日期 | 2026-09-20 |
| 平台 | 仅 Windows |
| 技术 | .NET 8 + WPF + WebView2 |
| 一期源 | 720 云 |
| 导出 | 瓦片包 + 整图 |
| 预览 | 缩略图（3D 二期） |
| 登录 | 软件内 WebView2 官方登录页 |
| 结论 | **按本文档开发** |

---

## 13. 下一步与进度

| 阶段 | 状态 |
|------|------|
| 阶段 0 需求确认 | ✅ 完成（v0.2） |
| 阶段 1 WPF + WebView2 + 登录态 | ✅ 完成 |
| 阶段 2 720 云完整检测 | ✅ 基本完成（公开/登录作品均可分析出 Manifest；多场景切换后需再分析） |
| 阶段 3 瓦片下载 | ✅ 完成（并发/进度/重试/断点续传/取消；失败可再次导出跳过已成功项） |
| 阶段 4 整图拼接 | ✅ 完成（立方体面拼接 + equirect 预览调整；矩阵投影直接出整图；`manifest.json` 双模式导出） |
| 阶段 5 打磨与安装包 | ✅ 完成（设置页、关于/声明、缩略图加载态、日志复制、WebView2 检测、win-x64 单文件发布） |

**阶段 2 当前交付：**

- 网络请求捕获（WebView2 + CDP `Network.getResponseBody` 拉取 JSON 正文）
- `Yun720ConfigExtractor`：从作品/场景 API 与页面文本提取 `/imgs/` 根路径与场景列表
- `Yun720TileInferencer`：
  - 立方体多分辨率 `face/lN/rr/lN_face_rr_cc.jpg`
  - 平面矩阵多分辨率 `imgs/lN/rr/lN_rr_cc.jpg`
  - 仅有配置根路径时补默认立方体层级
- 场景噪声过滤；缩略图补全；输出统一 `PanoramaManifest`
- 左侧「仅多瓦片」过滤开关

**阶段 3 / 4 当前交付：**

- `TileDownloader`：信号量并发、Referer/Cookie、超时重试、已存在非空文件跳过（续传）
- `ExportService`：按 Manifest 最高层下载 → 网格探测扩展 → 写 `manifest.json` → 瓦片包 / 整图 / 两者
- `CubeFaceStitcher` + `EquirectProjector` + 预览窗：立方体六面手动换位/旋转后导出 equirect
- `MatrixTileStitcher`：平面矩阵瓦片直接拼成 equirect（无需立方体预览）
- UI：导出进度分阶段、取消导出、导出历史、尊重 `OpenFolderAfterExport` / `MaxEquirectWidth`

**阶段 5 当前交付：**

- 设置窗：下载目录、并发、超时、重试、默认导出模式、JPEG 质量、整图最大宽度、导出后打开文件夹
- 关于页：版本、免责声明、WebView2 Runtime 状态与下载引导
- 首次启动使用声明（已有）+ 工具栏「设置 / 关于」
- 缩略图加载态；日志复制 / 清空；「默认导出」按钮
- 启动时检测 WebView2 Runtime，缺失则引导安装
- `pack/publish.ps1`：测试通过后发布 win-x64 单文件至 `dist/win-x64`

**使用提示：** 打开作品并等待全景瓦片加载后点「分析」。多场景请在网页中逐一切换场景后再分析。导出前可在「设置」调整目录与并发。

**MVP 已达交付状态；后续按需迭代（二期 3D 预览 / 其它平台等）。**
